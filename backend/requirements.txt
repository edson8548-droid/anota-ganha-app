# SUBSTITUA: backend/requirements.txt
# Este é o teu ficheiro + o que faltava

# FastAPI e servidor
fastapi
uvicorn[standard]

# Banco de dados MongoDB
motor
pymongo
dnspython # ⭐️ ADICIONADO: Necessário para MongoDB Atlas

# Autenticação e segurança
bcrypt
PyJWT
python-multipart

# Modelos e validação
pydantic
pydantic-settings

# Variáveis de ambiente
python-dotenv

# Email (SendGrid)
sendgrid

# HTTP requests
requests

# Timezone e datas
python-dateutil

# Pagamentos (Mercado Pago)
mercadopago # ⭐️ ADICIONADO

# NOVO: backend/requirements.txt

# ... (restante do teu ficheiro) ...

# Firebase Admin SDK para acesso ao Firestore
firebase-admin

# IA — Google Gemini Flash
google-generativeai

# Motor de matching — Robô de Cotação
rapidfuzz
openpyxl
pandas

# Leitura de PDF (tabelas de preços dos atacadistas)
pdfplumber
cryptography>=46.0.7 # not directly required, pinned by Snyk to avoid a vulnerability
pdfminer.six>=20251107 # not directly required, pinned by Snyk to avoid a vulnerability
pillow>=10.3.0 # not directly required, pinned by Snyk to avoid a vulnerability